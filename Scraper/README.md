# Internship Scraper

## Setup

```bash
pip install -r requirements.txt
```

## Usage

```bash
python main.py
```

This will create a `jobs.json` file in the project root with a unified list of internships.

